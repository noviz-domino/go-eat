# 📓 가서 먹어 (GoEat) 개발일지

> 기획부터 배포까지의 과정을 시간순으로 기록한다.
> 새 작업이 생길 때마다 이 파일 아래에 이어서 추가한다.

---

## 2026-08-03

### 1. 기획 — 서비스 방향 확정

- 처음엔 "가고 싶은 맛집 저장·체크" (도시 어디서나 통하는 범용 컨셉)로 출발
- 논의 중 방향 전환: **"시골은 애초에 리뷰·평점 데이터가 없다"** 는 진짜 문제로 초점을 좁힘
  → 시골에서는 검색으로 맛집을 못 찾고, **직접 가보고 기록하는 것 말고는 방법이 없음**
  → 이 지점이 "왜 외부 API 대신 직접 입력을 쓰는가"에 대한 논리적 근거가 됨
- 서비스명 후보 검토: 맛집 도장깨기 → 가봐야알지 → **가서 먹어 (GoEat)** 로 최종 확정
  - "가봐야알지"의 뜻(지도에 없으니 가봐야 안다)을 살리면서 더 강한 명령조로 정리

### 2. 기획서 작성 (`기획서.md`)

다음 항목을 확정:

| 항목 | 결정 |
| --- | --- |
| 서비스명 | 가서 먹어 (GoEat) |
| 한 줄 정의 | 검색해도 안 나오는 시골 맛집을 직접 저장하고, 다녀온 곳을 체크·평가해 나만의 맛집 목록을 만드는 앱 |
| 테이블 | `restaurants` (id, name, category, address, rating, visited, visited_at, memo, user_id, created_at) |
| 화면 | 로그인 · 목록 · 상세 · 등록/수정 |
| 안 만들 것 | 지도, 사진, 외부 API, 소셜 로그인, 팔로우, 댓글, 추천 알고리즘, 쿠폰 |
| 개발 시간 | 8시간 (실습 기준) |

**논의하며 정한 세부 결정**

- 등록/수정 화면은 **파일을 분리**하기로 함 (공용 컴포넌트로 합치지 않음)
  → 이유: 분기 없이 파일 하나로 화면이 완결되어야 코드를 직접 읽으며 검토하기 쉽다는 판단
  → 단, 별점 입력 UI(`<StarRating>`)만 공용 컴포넌트로 분리
- `category`는 고정 6종 드롭다운 (한식/중식/일식/양식/카페·디저트/기타) — 자유 입력 금지
- 시골 맛집 특유의 정보(현금만 받음, 재료 소진 시 마감 등)는 별도 컬럼 없이 `memo`에 자유 기술
- 지도·사진 기능은 **8시간 실습 범위에서 제외, 2일차 확장으로 이동**
  - 지도: Leaflet + OpenStreetMap (API 키 불필요) + 카카오 주소검색 API로 좌표 자동 변환 예정
  - 참고 사이트: https://restaurant.coroke.net/ (Leaflet 기반 마커 지도)

### 3. 개발 환경 세팅

- 로컬 폴더 이름을 `맛집실습` → `GoEat`(중간 시도) → **`go_eat`** 으로 최종 확정
  - 한글/대문자 폴더명은 npm 패키지명 규칙과 충돌해 문제가 됨을 확인
- 현재 위치(`C:\Users\HOME\Desktop\git\go_eat`)에 Next.js 프로젝트 생성
  - 옵션: TypeScript, Tailwind CSS, ESLint, App Router, `src/` 디렉토리, `@/*` import alias
  - 이번 8시간 실습 단계에서는 **Next.js 프로젝트 생성까지만** 진행 (강사 진도에 맞춤)

### 4. GitHub 저장소

- 저장소: [`noviz-domino/go-eat`](https://github.com/noviz-domino/go-eat) (Public)
- 기본 브랜치를 `master` → `main`으로 변경
- 커밋 이력
  - `c3c189f` Initial commit from Create Next App
  - `97f7369` Add project plan document (기획서.md 추가)
  - `30ceee3` Replace default Next.js starter page with project title

### 5. Vercel 배포

- GitHub 저장소를 Vercel(`AIAgent01` 팀)에 Import → 자동 배포
- 최초 배포 URL(`go-eat-xi.vercel.app`)의 `-xi` 접미사 원인 확인
  → `go-eat.vercel.app`은 이미 다른 사용자가 선점(전역 네임스페이스라 충돌 시 랜덤 접미사 부여)
- `Settings → Domains`에서 커스텀 서브도메인 `go-eat-noviz.vercel.app` 추가
  - 기존 `go-eat-xi.vercel.app`은 새 도메인으로 307 리다이렉트되도록 유지
- **최종 배포 주소:** https://go-eat-noviz.vercel.app

### 6. MCP 서버 연결

- Vercel MCP: 배포 상태 확인, 도메인·보호 설정 조회에 사용
- Supabase MCP: 프로젝트 스코프로 추가, 인증 완료

### 7. Supabase 테이블 생성 + 테스트 데이터

- 기존 Supabase 프로젝트(`noviz-domino's Project`, `ap-southeast-1`) 사용
- 기획서 스키마 그대로 `restaurants` 테이블 생성
  - `id, name, category, address, rating, visited, visited_at, memo, user_id, created_at`
  - ⚠️ 로그인 기능이 아직 없어 `user_id`는 **임시로 nullable** 처리
    → 로그인 화면 구현 시 `NOT NULL` + RLS 정책(기획서 SQL 참고)으로 되돌려야 함
- 동작 확인용 시골 맛집 3건 삽입 (봉평면 메밀국수, 읍내 중국집, 할매 손칼국수)

### 8. 메인 페이지에서 Supabase 데이터 표시

- `@supabase/supabase-js` 설치
- `src/lib/supabase.ts` — Supabase 클라이언트 + `Restaurant` 타입 정의
- `src/app/page.tsx` — 서버 컴포넌트에서 `restaurants` 테이블을 조회해 목록으로 표시
  - ⚠️ 처음 빌드했을 때 이 페이지가 **정적(Static) 페이지**로 고정되는 문제 발견
    → 빌드 시점의 데이터만 굳어버려서, 나중에 등록 기능이 생겨도 화면이 갱신되지 않음
    → `export const dynamic = "force-dynamic"` 추가로 매 요청마다 새로 조회하도록 수정
  - 로컬 프로덕션 빌드(`npm run build && npm run start`)로 실제 데이터 노출까지 확인
- 환경변수 `.env.local` 생성 (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)
  - `.gitignore`에 이미 `.env*`가 있어 커밋되지 않음 확인
- Vercel CLI로 로컬 프로젝트를 `aia-gent9/go-eat`에 연결(`vercel link`)
- 같은 환경변수를 **Vercel Production·Preview 환경에 등록** (`vercel env add`)
- 커밋 `a8d3b5b` → `main` 푸시 → Vercel 자동 배포 트리거

### 9. 로그인 기능 + RLS 적용

**시작 전에 확인한 것 — Next.js 16의 달라진 점**

`AGENTS.md`의 지시에 따라 코드 작성 전 `node_modules/next/dist/docs/`를 먼저 확인했다.
그 결과 **Supabase 공식 문서를 그대로 따라 하면 안 되는 지점**을 발견했다.

| 항목 | 일반적인 안내 (Next 15 이하) | **Next.js 16** |
| --- | --- | --- |
| 요청 가로채는 파일 | `middleware.ts` | **`proxy.ts`** (이름이 바뀜, 기능은 동일) |
| `cookies()` | 동기 함수 | **async 함수** (`await cookies()`) |

> 문서를 안 읽고 `middleware.ts`를 만들었다면 **파일이 아예 실행되지 않아서**
> "왜 로그인이 유지가 안 되지?"를 한참 헤맸을 것이다.

**세션 방식 결정 — 쿠키 기반 (`@supabase/ssr`)**

- 대안: 브라우저 localStorage에만 세션을 두는 간단한 방식
- 쿠키 방식을 택한 이유: 서버가 "누가 로그인했는지" 알 수 있어야
  ① 서버에서 내 데이터만 걸러 보내고 ② 비로그인 접근을 서버에서 차단할 수 있다.
  나중에 실제 서비스로 확장할 때 뜯어고치지 않으려면 처음부터 이 구조여야 한다.

**추가/변경한 파일**

| 파일 | 역할 |
| --- | --- |
| `src/lib/supabase/server.ts` | 서버용 클라이언트 (쿠키에서 세션 읽기) |
| `src/lib/supabase/client.ts` | 브라우저용 클라이언트 |
| `src/lib/types.ts` | `Restaurant` 타입 + 카테고리 6종 (기존 `src/lib/supabase.ts` 대체) |
| `src/app/actions/auth.ts` | 로그인·가입·로그아웃 Server Action |
| `src/app/login/page.tsx` | 로그인 화면 (이미 로그인 상태면 `/`로 보냄) |
| `src/app/login/login-form.tsx` | 폼 (React 19 `useActionState`로 에러 표시) |
| `src/proxy.ts` | 세션 자동 갱신 + 비로그인 시 `/login` 리다이렉트 |
| `src/app/page.tsx` | 내 맛집만 표시 + 진행률 + 빈 상태 화면 |

- `page.tsx`의 `export const dynamic = "force-dynamic"`은 **제거**했다.
  `cookies()`를 쓰면 Next.js가 자동으로 동적 렌더링으로 전환하기 때문에 불필요해짐.
  (빌드 결과에서 `/`가 `ƒ`로 표시되는 것으로 확인)

**이메일 인증 설정**

- Supabase 기본값은 인증 **켜짐**(`mailer_autoconfirm: false`)이었다.
- 무료 프로젝트의 메일 발송은 **시간당 2건 제한**이라 실습 중 계정 테스트가 막힌다.
  → 대시보드에서 `Authentication → Sign In/Providers → Email → Confirm email` **OFF**
- 단, **코드는 양쪽 다 처리**하게 작성했다. 인증이 켜져 있으면 세션이 없는 채로
  가입만 되므로 "가입 확인 메일을 보냈습니다" 안내를 띄운다.
  → **실제 서비스로 갈 때 설정만 다시 켜면 되고 코드 수정은 없다.**

**데이터베이스 변경 (마이그레이션 `enable_rls_on_restaurants`)**

```sql
delete from restaurants where user_id is null;        -- 주인 없는 테스트 3건 정리
alter table restaurants alter column user_id set not null;
alter table restaurants enable row level security;
-- select / insert / update / delete 4개 정책 모두 auth.uid() = user_id
```

**실제 동작 검증 (브라우저로 확인)**

| 확인 항목 | 결과 |
| --- | --- |
| 비로그인 상태로 `/` 접속 | `proxy.ts`가 `/login`으로 리다이렉트 ✅ |
| `test1@goeat.test` 가입 | 즉시 로그인 → 빈 상태 화면 표시 ✅ |
| 맛집 3건 삽입 후 재접속 | 목록 + "3곳 중 2곳 정복" 표시 ✅ |
| 로그아웃 | `/login`으로 이동 ✅ |
| **`test2@goeat.test`로 가입** | **맛집 0개 — test1의 데이터가 안 보임** ✅ RLS 작동 |
| Supabase 보안 점검 재실행 | 경고 **0건** (이전엔 `rls_disabled_in_public` ERROR) ✅ |

**부수 작업**

- 포트 3000에 이전 테스트용 `next start` 프로세스가 살아있어 종료함
  (`pkill`이 Windows에서 안 먹혀서 PID로 확인 후 `Stop-Process`)
- `.claude/launch.json` 추가 — 개발 서버를 일관되게 띄우기 위한 설정

---

## 다음에 할 일

- [ ] 등록 화면 (`/restaurants/new`) — 지금은 RLS 때문에 Table Editor로 직접 못 넣음
- [ ] 상세 화면 (`/restaurants/[id]`) + 수정 · 삭제
- [ ] 검색 · 방문 탭 · 카테고리 필터
- [ ] 로딩 · 에러 처리
- [ ] 디자인 다듬기 (기획서의 디자인 가이드 참고)
- [ ] 테스트 계정(`test1`, `test2`) 정리
